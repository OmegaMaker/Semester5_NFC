apiVersion: apps/v1
kind: Deployment
metadata:
  name: validate-access-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: validate-access-test
  template:
    metadata:
      labels:
        app: validate-access-test
    spec:
      containers:
      - name: placeholder
        image: busybox
        command: ["sh", "-c", "while true; do echo running; sleep 10; done"]
