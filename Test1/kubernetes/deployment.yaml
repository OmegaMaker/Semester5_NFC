# kubernetes/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: receiver-deployment # Name of deployment
  labels:
    app: receiver
spec:
  replicas: 2 # Scale for availability
  selector:
    matchLabels:
      app: receiver
  template:
    metadata:
      labels:
        app: receiver
    spec:
      containers:
      - name: receiver-container
        image: europe-north2-docker.pkg.dev/$PROJECT_ID/nfc-images/receiver:latest # Placeholder image
        ports:
        - containerPort: 8000 # The port the Flask app listens on inside the container
        # Add any environment variables, resource limits, etc. here if needed
        # env:
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
